# Bluetm Agent · LLM proposal

_Generated by claude-cli at 20260523-005422_

---

## Target file

`D:\Code\mytools\cli.py` â€” `infer_kind` (lines 43â€“63)

## Rationale (under 80 words)

`cli.py`'s docstring advertises `osint 8.8.8.8 â†’ IP (rDNS + IPinfo)`, but `infer_kind` never returns `QueryKind.IP`: IPv4 only reaches the ip module because it accidentally matches `_DOMAIN_RE` and ip.py rescues it via `_is_ip`. **IPv6 falls through entirely** â€” `2001:db8::1` is classified as `USERNAME` and the ip module isn't dispatched, so it gets blasted against 1000+ username probes. Fix uses stdlib `ipaddress`, no new dependency, no schema/flag change.

## Diff

```diff
--- a/cli.py
+++ b/cli.py
@@ -17,6 +17,7 @@
 from __future__ import annotations

 import argparse
 import asyncio
 import csv
+import ipaddress
 import io
 import json
 import os
@@ -50,6 +51,10 @@ _DOMAIN_RE = re.compile(

 def infer_kind(value: str) -> QueryKind:
     v = value.strip()
+    try:
+        ipaddress.ip_address(v)
+        return QueryKind.IP
+    except ValueError:
+        pass
     if _EMAIL_RE.match(v):
         return QueryKind.EMAIL
     digits = re.sub(r"\D", "", v)
```

Existing `tests/test_cli.py` cases (`torvalds`, `@durov`, `a@b.co`, `+998 â€¦`, `example.com`, `sub.example.co.uk`) all still resolve correctly â€” none parse as IPs. The same bug exists verbatim in `app/ui/main_window.py:53` and `app/ui/interactive.py:74`; flagging here but keeping the proposal scoped to one file as requested.
